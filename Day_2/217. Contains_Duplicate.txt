class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        num=sorted(nums)
        for i in range(1,len(num)):
            if num[i]==num[i-1]:
                return True
        else:
            return False